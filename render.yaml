services:
  - type: web
    name: dreamslab-cloud-vault
    runtime: node
    rootDir: server
    buildCommand: npm install --omit=dev
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: https://abdullahinayat24-lang.github.io
      - key: DATABASE_URL
        fromDatabase:
          name: dreamslab-vault-db
          property: connectionString

databases:
  - name: dreamslab-vault-db
    plan: free
    databaseName: dreamslab_vault
    user: dreamslab
